package ch4

// When you name a package, via the `package` keyword, you provide a single value, not
// a complete hierarchy (e.g., "shopping" or "db").

// When you import a package, you specify the complete path (from $GOPATH/src).

func Are_packages_cool() bool {
	return true
}
